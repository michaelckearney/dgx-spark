---
- name: Configure DGX Spark workstation
  hosts: localhost
  connection: local
  become: true
  gather_facts: true

  roles:
    - docker
    - compose
    - tooling
    - chezmoi
    - reconcile
